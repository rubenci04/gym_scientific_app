name: gym_scientific_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.5+1
  fl_chart: ^0.63.0
  intl: ^0.18.1
  # Nueva dependencia para dibujar el mapa corporal
  path_drawing: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Nota: Bajé esto a 3.0.0 porque la 6.0.0 puede dar problemas si tu SDK no es el más nuevo (Dart 3.3). Si te da error, prueba con 2.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true

  #   - assets/images/logo.png