name: gym_scientific_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
  
  # --- AQUÍ ESTÁ LA CLAVE ---
  assets:
    # Registramos las carpetas normalizadas (minúsculas)
    - assets/images/body/male/front/
    - assets/images/body/male/back/
    # Descomenta estas cuando subas las imágenes de mujer
    # - assets/images/body/female/front/
    # - assets/images/body/female/back/